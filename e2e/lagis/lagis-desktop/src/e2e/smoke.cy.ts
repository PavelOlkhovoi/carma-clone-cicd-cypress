// Temporary always-pass smoke test for LagIS.
// Remove once real smoke checks are stable.
describe('lagis smoke test (temporary always-pass)', () => {
  it('always passes', () => {
    expect(true).to.eq(true);
  });
});
